node_modules/
.next/
out/
.env
.env.local
.env.production
*.log
.DS_Store
coverage/
.vscode/
.idea/
next-env.d.ts
tsconfig.tsbuildinfo

# Standalone Remotion promo-video project — accidentally swept into the repo
# (216MB of mp4/png/wav assets). Not part of the app; lives on disk only.
/promo/

# Binary doc exports — keep the .md sources, ignore the generated .docx/.pdf.
/docs/*.docx
/docs/*.pdf

# Playwright e2e artifacts + captured session state (never commit the auth cookie).
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
/tests/e2e/.auth/

# Local-only dev artifacts (audit zips, old scripts, promo media) — kept on disk
# for convenience but never part of the shippable/sellable product.
/_archive/
